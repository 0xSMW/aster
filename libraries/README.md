# libraries/

This directory holds non-committed third-party reference clones used during
development (e.g. tinygrad for ML port planning).

If tinygrad is not available, you can clone it from the following URL:
https://github.com/tinygrad/tinygrad.git

Contents are gitignored (see `.gitignore`).
